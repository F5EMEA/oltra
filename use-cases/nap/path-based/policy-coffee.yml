apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: coffee-policy
  namespace: nap-vs
spec:
  waf:
    enable: true
    apPolicy: "na-vs/nap-coffee"
    securityLogs:
    - enable: true
      apLogConf: "nap-vs/logconf"
      logDest: "syslog:server=10.1.1.7:515"
